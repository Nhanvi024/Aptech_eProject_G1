import './LogIn.css'

import React from 'react';

function LogIn(props) {

    return (
        <div className='loginContainer'>
            <button className='loginBtn'>Log in</button>
            <button className='singinBtn'>Sign in</button>
        </div>
    );
}

export default LogIn;
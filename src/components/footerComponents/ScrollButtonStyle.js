import styled from 'styled-components'; 
   
  
export const Button = styled.div` 
   position: fixed;  
   width: 100%; 
   left: 90%; 
   bottom: 40px; 
   height: 100px; 
   font-size: 5rem; 
   z-index: 1; 
   cursor: pointer; 
   color: hsl(var(--main-color-code)); 
`